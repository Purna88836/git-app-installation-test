name: Production

deployment_branch_policy:
  protected_branches: true
  custom_branch_policies:
    - name: "main"
      required_status_checks:
        - "ci/test"
        - "ci/lint"

reviewers:
  - team: @ops-team